@model IEnumerable<MVC_EF_Start.Models.Review>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.reviewer_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.review_Details)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.review_rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.item_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.reviewer_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.review_Details)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.review_rating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.item_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.review_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.review_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.review_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
